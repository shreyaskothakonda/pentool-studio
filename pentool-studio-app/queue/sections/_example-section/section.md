---
name: _example-section          # must match the folder name
build: static                  # static | component

# group: GCE                   # component group, when build: component
#
# props:                       # only when build: component
#   - name: Title
#     type: textContent        # textContent string richText image link video number boolean id altText
#     target: example_title    # a class that appears in the dump below
#   - name: Show badge
#     type: boolean
#     target: example_badge
#     binds: visibility
#
# cms:                         # bind to an existing collection
#   collection: Markets
#   element: example_list      # this element becomes the Collection List
#   limit: 12
#   bind:                      # element class -> collection field slug
#     example_title: name
#     example_text: summary
---

Paste the Pentool Studio output below this line, with the target set to
"Webflow · Client-First". Delete this paragraph.

=== BUILD CONTRACT (webflow / client-first) ===
